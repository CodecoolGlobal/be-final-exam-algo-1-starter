name: CC Autograde

on:
  push:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Set up JDK 11
      uses: actions/setup-java@v1
      with:
        java-version: 11
    - name: Build with Maven
      run: mvn clean test
      env:
        AID: "5634161670881280"
        CID: "5f3ffdd9-e102-4296-b835-468897f7a0dd"
                                    
